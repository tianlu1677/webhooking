let dom = document.getElementById("card-list-group")

if (dom) {
  dom.innerHTML = `<%= j render '/backpacks/left_list_item', backpack: @backpack, current_backpack: nil%>${dom.innerHTML}`

}

<%if @backpack %>
const ele = document.getElementById('backpacks-count')
ele.innerHTML = parseInt(ele.textContent) + 1

<%end %>