.row
  .col-md-7
    .card
      .card-header
        h4 编辑自定义内容 
        = link_to "返回列表", webhook_custom_actions_path(@webhook)
      .card-body
        == render 'form'  
    => link_to 'Show', webhook_custom_action_path(@webhook.uuid, @custom_action)
    '|
    =< link_to 'Back', webhook_custom_actions_path(@webhook.uuid)
  .col-md-5
    = render "incoming_variable_list"
